import React from 'react';

const <%= name[0].toUpperCase() + name.slice(1).toLowerCase() %>Page = () => {
  return (
    <div><%= name %> page</div>
  );
}

export default <%= name[0].toUpperCase() + name.slice(1).toLowerCase() %>Page;
