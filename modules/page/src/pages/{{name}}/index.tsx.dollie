import React from 'react';

interface <%= name[0].toUpperCase() + name.slice(1).toLowerCase() %>PageProps {}

const <%= name[0].toUpperCase() + name.slice(1).toLowerCase() %>Page: React.FC<<%= name[0].toUpperCase() + name.slice(1).toLowerCase() %>PageProps> = () => {
  return (
    <div><%= name %> page</div>
  );
}

export default <%= name[0].toUpperCase() + name.slice(1).toLowerCase() %>Page;
